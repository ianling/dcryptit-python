from setuptools import setup
setup(
  name = 'dcryptit',
  packages = ['dcryptit'],
  install_requires = ['requests'],
  version = '1.0',
  description = 'A library for interacting with dcrypt.it',
  author = 'Ian Ling',
  author_email = 'ian@iancaling.com',
  url = 'https://github.com/ianling/dcryptit-python',
  download_url = 'https://github.com/ianling/dcryptit-python/archive/1.0.tar.gz',
  keywords = ['parse', 'dlc', 'decryptit', 'decrypt'],
  classifiers = [],
)
